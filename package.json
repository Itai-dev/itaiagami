{
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "ffmpeg-static": "^5.3.0",
    "three": "^0.186.0"
  },
  "scripts": {
    "build:gummies": "esbuild src/hero-gummies.js --bundle --format=esm --minify --legal-comments=inline --target=es2020 --outfile=js/hero-gummies.min.js"
  }
}
